# game-explorer
A flutter project created using bloc pattern as its state management.


packages used:

**Freezed**
pub.dev : https://pub.dev/packages/freezed_annotation/install <br>
dependencies : <br>
  freezed_annotation: ^1.1.0 <br>
  freezed : any <br>
  build_runner : any
  
  
  the package is used to generated boiler plate template that is helpful to speed up 
  the development process.
  
